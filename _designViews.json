{
   "_id": "_design/data",
   "_rev": "9-45b847166ba2c3a56372ddee0e838628",
   "language": "javascript",
   "views": {
       "stats": {
           "reduce": "_stats",
           "map": "function(doc) {for (var key in doc){val=parseFloat(doc[key]);if (key !== '_id' && key !== '_rev' && !isNaN(val)){emit([doc.component,key, doc.version], val);}}}"
       },
       "data": {
           "map": "function(doc){emit(doc.component, doc);}"
       }
   }
}